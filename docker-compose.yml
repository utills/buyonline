services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: buyonline
      POSTGRES_PASSWORD: buyonline
      POSTGRES_DB: buyonline
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
